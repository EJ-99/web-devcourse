import React from 'react';
import styled from 'styled-components';

export default function BooksViewSwitcher() {
  return <BooksViewSwitcherStyle></BooksViewSwitcherStyle>;
}

const BooksViewSwitcherStyle = styled.div``;
